---
- name: Install QEMU on Ubuntu hosts
  become: yes # Use sudo
  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: yes

    - name: Install QEMU system packages
      ansible.builtin.apt:
        name:
          - qemu-system
          - qemu-utils
        state: present
